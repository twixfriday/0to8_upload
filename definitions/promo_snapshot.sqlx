config {
    type: "table",
    schema: "analytics",
    name: "promo_snapshots"
}
SELECT *
FROM ${ref("tiktok_snaps")} AS snap
LEFT JOIN ${ref("promo_exp")} AS exp
  ON snap.promo_expense_id = exp.coda_row_id